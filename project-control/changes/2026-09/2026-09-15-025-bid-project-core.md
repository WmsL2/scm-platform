# 变更记录：投标项目核心与 Excel 基础设施

变更编号：2026-09-15-025
模块：投标项目
日期：2026-09-15
分支：`feat/bid-project-core`

## 目标

实现 1 号任务：投标项目核心、通用 Excel 模板识别、源文件与报价文件版本、项目生命周期和共享数据库结构。

## 已完成

- 新增投标项目八张共享表、`BID_PROJECT` 并发安全业务编号序列和九项投标权限。
- 新建项目时保存 ORIGINAL 文件，计算文件摘要，识别模板后解析需求行；未识别或解析失败时保留项目和源文件并记录导入状态。
- 提供项目列表、详情、需求行服务端分页、文件列表与下载、报价导出、标记投标、中标、未中标接口。
- 报价导出始终以 ORIGINAL 副本生成，版本递增，源文件不被覆盖。
- 项目状态变化写入事件历史；非法状态变化返回 409。
- 新增 `ADR-0019` 固化模板、文件版本和重新选品后的导出规则。

## 数据库

- 新增 Revision：`20260915_0024`，前序为 `20260914_0023`。
- 本地数据库已从 `0023` 升级到 `0024`，当前为单一 Head。

## 权限

新增：`bid:list`、`bid:detail`、`bid:create`、`bid:match`、`bid:select`、`bid:export`、`bid:file:download`、`bid:submit`、`bid:result`。

上述权限已默认授予现有管理员角色 `boss`；普通用户角色不自动获得权限。

## 验证

- `python -m ruff check .`：通过。
- `python -m mypy app`：通过。
- `python -m pytest -q tests/bid`：3 项通过。
- `python -m alembic upgrade head`：通过，当前 Head 为 `20260915_0024`。

## 未完成事项

- 未提供真实买家样表，因此未预置任何客户模板和实际报价写入列。
- 商品匹配、候选、人工选品由 2 号任务实现；投标工作台由 3 号任务实现。
