# 2026-09-22-006：商品导出价格保留两位小数

- 商品主数据自选字段导出的成本价、市场价、京东价、协议价、协议价采购价、利润及京东自营前台价，按 `ROUND_HALF_UP` 四舍五入到分，并以 Excel `0.00` 格式显示。
- 通常写入数值单元格；超过 Excel 约 15 位有效数字、无法可靠保留分的极大金额以两位小数文本写入，避免静默丢失分位。空值保持空白。
- 仅影响供查看的商品导出文件，不回写数据库，也不改变商品导入、正式模板、百分比或 SKU。
- 增加导出单元格实际值、格式、边框和其他字段不变的回归测试。
- Alembic Revision：无；API 路径、请求体、权限和前端 UI：无变化。响应文件中的价格单元格由文本改为两位小数数值（极大金额例外）。
