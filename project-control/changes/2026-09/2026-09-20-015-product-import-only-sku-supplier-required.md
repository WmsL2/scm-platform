# 2026-09-20-015：商品导入仅 SKU 与供应商必填

## Change

- 固定 43 列模板保持不变；仅 SKU 与供应商单元格必填，其他 41 列空白统一写为 `NULL`。
- 取消三级类目导入与手工编辑的必填拦截；类目筛选候选按层级兼容部分路径。
- 保留 ADR-0030 数值容错/公式精度保护、ADR-0032 折扣率规则、SKU 防重、供应商解析、图片与日期校验。

## Tests

- 新增仅 SKU + 有效供应商导入、四种独立空类目组合、手工清空类目和一级候选回归测试。

## Migration

无。现有类目字段已允许 `NULL`。
