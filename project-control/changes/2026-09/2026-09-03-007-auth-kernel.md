# Change Record: Auth Kernel

Change ID: 2026-09-03-007
Module: auth-rbac
Branch: feat/auth-kernel

实现 Auth Kernel：Argon2id、HS256 access token、token_version、CurrentUser、require_permission 与 login/me/logout。新增 Revision 20260903_0002，创建五张 sys_* 表，无默认用户/密码/secret。

最终验收：pytest 30 passed（Foundation 13 + Auth 17，Warnings 0）；登录不存在用户、错误密码、DISABLED 与 deleted 用户均返回相同 401 / `AUTH_INVALID_CREDENTIALS` / `Invalid credentials`，不比较动态 request_id。JWT decode 显式要求 `sub`、`ver`、`iat`、`exp`，API 级测试使用当前 Settings 的测试 Secret，分别验证每个 claim 缺失及过期 Token。已签发 Token 在用户变为 DISABLED 或 `is_deleted=true` 后立即被 `/me` 拒绝。真实 MySQL 8 Integration Test 验证活跃用户名生成列唯一性允许多条删除历史、FK 与 RESTRICT、CHECK 实际拒绝非法值、UNIQUE/辅助索引及 UUID CHAR(36) 合同；Role 逻辑删除会即时从 roles 与 permissions 排除，且无需重签 Token。

排除 Supplier、Business Sequence、Refresh Token、Session 与前端页面。未创建 Supplier/Product/Quote 表，未修改商品主数据冻结规则。Pending：Supplier Gate、Business Sequence、Refresh Token。
